name: Publish passport-worker

on:
  workflow_dispatch:
  push:
    tags:
      - 'passport-worker-*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    env:
      PYTHON_VERSION: 3.14
      ASTRAL_VERSION: 0.11.6
    steps:
      - uses: actions/checkout@v6
      - name: Create GH release
        run: gh release create "$tag" --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}

  publish-passport-worker-to-pypi:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    environment:
      name: pypi
      url: https://pypi.org/p/datashare-passport-worker
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.10.8"
      - name: Install Python 3.11
        run: uv python install 3.11
      - name: Build
        run: cd workers/passport-worker && uv build
      - name: Publish
        run: cd workers/passport-worker && uv publish --verbose

  publish-io-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: icij/datashare-passport-io-worker
          tags: |
            type=match,pattern=passport-worker-(.*),group=1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          # You'll need to set these secrets
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          target: passport-io-worker
          context: ./workers/passport-worker
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  publish-passport-preprocessing-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: icij/datashare-passport-preprocessing-worker
          tags: |
            type=match,pattern=passport-worker-(.*),group=1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          # You'll need to set these secrets
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          target: passport-preprocessing-worker
          context: ./workers/passport-worker
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  publish-passport-inference-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: icij/datashare-passport-inference-worker
          tags: |
            type=match,pattern=passport-worker-(.*),group=1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          # You'll need to set these secrets
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          target: passport-inference-worker
          context: ./workers/passport-worker
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  trigger-workflows-worker-publication:
    needs:
      - publish-passport-worker-to-pypi
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Trigger workflow worker release
        run: |
          package_version="${package_version/passport-worker-/}"
          gh workflow run .github/workflows/publish_workflows_worker.yml -f package="$package" -f package_version="$package_version" --ref main
        env:
          GH_TOKEN: ${{ github.token }}
          package_version: ${{ github.ref_name }}
          package: datashare-passport-worker

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false
