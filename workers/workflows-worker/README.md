# Datashare all-in one workflow worker

Single Temporal workflow worker to run all datashare workflows.

This is mainly just `pyproject.toml` used to generate `uv.lock`/`uv.dist.lock` files and helps with worker
docker image build.